# Pandas and inner join database

Using pandas, data was read from the "base .db" database. 

Then an INNER JOIN of the table of cards with people was performed 
and recorded partitioned in .parquet format